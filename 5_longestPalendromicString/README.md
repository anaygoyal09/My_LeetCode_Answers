# Longest Palindromic String

## Problem Description

Given a string `s`, return the longest palindromic substring in `s`.

## Solution

Expand around the center for each character (and between characters) to find the longest palindrome.
